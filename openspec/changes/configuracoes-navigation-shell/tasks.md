## 1. Static Data Modules

- [ ] 1.1 Criar `src/app/(app)/configuracoes/settings-areas.ts` — exportar `readonly` constant `SETTINGS_AREAS` tipada como `{ readonly label: string; readonly description: string; readonly href: string; readonly icon: LucideIcon; readonly slug: string }`. Entradas (em ordem): Locais de atendimento (`MapPin`, `/configuracoes/locais`, slug `locais`, "Endereços e modalidades onde você atende presencial ou online."), WhatsApp (`MessageCircle`, `/configuracoes/integracoes/whatsapp`, slug `whatsapp`, "Conecte sua conta do WhatsApp para enviar lembretes e mensagens."), Lembretes (`Bell`, `/configuracoes/lembretes`, slug `lembretes`, "Personalize quando e como avisar pacientes sobre suas sessões."), Agenda (`Calendar`, `/configuracoes/agenda`, slug `agenda`, "Horários de trabalho, duração padrão e regras de agendamento."). Microcopy com diacríticos exatos
- [ ] 1.2 **Teste unitário:** Criar `src/__tests__/unit/app/(app)/configuracoes/settings-areas.test.ts` — verificar: array tem 4 entradas, cada uma com label/description/href/slug não vazios, hrefs começam com `/configuracoes`, slugs são únicos, ícones são componentes Lucide válidos
- [ ] 1.3 Criar `src/app/(app)/configuracoes/integracoes/integrations.ts` — exportar `readonly` constant `INTEGRATIONS` com mesma forma de `SETTINGS_AREAS`. v1 com 1 entrada: WhatsApp (`MessageCircle`, `/configuracoes/integracoes/whatsapp`, slug `whatsapp`, "Conecte sua conta para enviar lembretes e mensagens.")
- [ ] 1.4 **Teste unitário:** Criar `src/__tests__/unit/app/(app)/configuracoes/integracoes/integrations.test.ts` — verificar: array tem 1 entrada (WhatsApp), shape correto, href válido
- [ ] 1.5 Criar `src/app/(app)/configuracoes/breadcrumb-labels.ts` — exportar `BREADCRUMB_LABELS` como `Record<string, string>` mapeando segmentos de URL para labels: `configuracoes`→"Configurações", `locais`→"Locais de atendimento", `integracoes`→"Integrações", `whatsapp`→"WhatsApp", `lembretes`→"Lembretes", `templates`→"Templates", `historico`→"Histórico", `agenda`→"Agenda"
- [ ] 1.6 **Teste unitário:** Criar `src/__tests__/unit/app/(app)/configuracoes/breadcrumb-labels.test.ts` — verificar: todas as labels têm cedilhas/diacríticos corretos onde aplicável, todos os segmentos URL usados em `SETTINGS_AREAS` e `INTEGRATIONS` têm label correspondente

## 2. Frontend — Sidebar Update (Modify app-shell)

> **Design System Salvia**: sidebar nav item "Configurações" com ícone `Settings` 20px (Lucide, stroke 1.5px), padding `space-2 space-3`, radius `md`, label cedilha-correto seguindo glossário fixo. Active state (em qualquer rota `/configuracoes/*`): text `brand-700`, bg `brand-50`, border-left 3px `brand-500`. Idle: text `text-secondary`. Hover: text `text-primary`, bg `surface`.

- [ ] 2.1 Atualizar `src/app/(app)/sidebar-nav.tsx` — alterar item de configurações de `{ label: 'Configuracoes', href: '/configuracoes/locais' }` para `{ label: 'Configurações', href: '/configuracoes' }`. Manter ícone `Settings`, manter posição última. A lógica `pathname.startsWith(item.href)` continua marcando ativo em todas as sub-rotas
- [ ] 2.2 **Teste unitário:** Criar `src/__tests__/unit/app/(app)/sidebar-nav.test.tsx` — verificar que o item renderiza com label "Configurações" (com diacríticos), linka para `/configuracoes` (não `/configuracoes/locais`), usa ícone `Settings`, e fica ativo (text-brand-700, bg-brand-50) em pathname `/configuracoes` e em sub-rotas como `/configuracoes/lembretes/templates`

## 3. Frontend — Settings Index Page

> **Design System Salvia**: h1 28px/600 "Configurações" (`text-text-primary`). Grid responsivo `grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`. Cards `interactive` (bg `surface`, border `border`, radius `xl`, shadow `xs`, padding `space-6`; hover border `border-strong`, cursor pointer). Cada card: ícone Lucide 20px `text-text-secondary` (decorativo, `aria-hidden`), label em h3 (18px/600 `text-text-primary`), description em body-sm (13px/400 `text-text-secondary`). Card inteiro envolto em `<Link>` com focus ring `focus-visible:shadow-focus focus-visible:outline-none`. Sem cards aninhados (DS proíbe). Tap target ≥ 44×44px em mobile.

- [ ] 3.1 Criar `src/app/(app)/configuracoes/page.tsx` — Server Component (sem `'use client'`) que importa `SETTINGS_AREAS` e renderiza `<h1>Configurações</h1>` (28px/600, `data-testid="settings-index-page-title"`) seguido de grid de cards interativos. Cada card: shadcn `Card` com classes `interactive` aplicadas (bg `surface` border `border` rounded-xl shadow-xs p-6 hover:border-border-strong cursor-pointer transition-colors duration-fast). Card envolto em `<Link href={area.href}>`. Conteúdo: ícone (20px `text-text-secondary` `aria-hidden`), label h3 18px/600, description body-sm 13px/400 `text-text-secondary`. Grid `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`. Container `data-testid="settings-index-page"`. Cada card `data-testid="settings-area-card-{slug}"`. Focus ring no link wrapper via `focus-visible:shadow-focus focus-visible:outline-none`. Não adicionar `mx-auto max-w-[1200px]` — o layout pai (task 4) provê
- [ ] 3.2 **Teste unitário:** Criar `src/__tests__/unit/app/(app)/configuracoes/page.test.tsx` — verificar: h1 "Configurações" renderiza, 4 cards com labels corretos renderizam, cada card linka para href correto, testids corretos, ícones Lucide presentes, descriptions têm microcopy exato com diacríticos

## 4. Frontend — Settings Layout + Breadcrumb

> **Design System Salvia**: nav breadcrumb com separadores `ChevronRight` 12px (Lucide, `aria-hidden`). Texto body-sm (13px) em `text-text-tertiary` para segmentos intermediários (Link com `hover:text-text-primary` transition `duration-fast`). Segmento atual (último) em `text-text-primary` font-medium com `aria-current="page"`. Container do layout: `mx-auto max-w-[1200px]` (token de largura geral do DS). Layout não duplica chrome do app-shell (header/sidebar herdados).

- [ ] 4.1 Criar `src/app/(app)/configuracoes/settings-breadcrumb.tsx` (Client Component, `'use client'`) — **Design system:** `<nav aria-label="Breadcrumb" data-testid="settings-breadcrumb">` contendo `<ol>` com `<li>` items. Lê `usePathname()`, splita em `/`, filtra a partir do segmento `configuracoes`, mapeia cada um para label de `BREADCRUMB_LABELS` (fallback para segmento cru em segmentos dinâmicos como `[templateKey]`). Separadores `ChevronRight` 12px `aria-hidden` em `text-text-tertiary`. Segmentos intermediários: shadcn `<Link>` em `text-text-tertiary` com `hover:text-text-primary transition-colors duration-fast`. Último segmento: `text-text-primary font-medium` com `aria-current="page"` (não-link). Texto inteiro em body-sm (13px). Em `/configuracoes` (índice), renderiza apenas "Configurações" como current (não-link)
- [ ] 4.2 **Teste unitário:** Criar `src/__tests__/unit/app/(app)/configuracoes/settings-breadcrumb.test.tsx` — verificar trilhas corretas para paths `/configuracoes`, `/configuracoes/locais`, `/configuracoes/lembretes/templates`, `/configuracoes/integracoes/whatsapp`, `/configuracoes/integracoes`. Verificar último segmento não-link com `aria-current="page"`. Verificar segmentos intermediários são `<Link>`. Verificar segmentos desconhecidos (ex.: `lembrete_24h`) renderizam como valor cru
- [ ] 4.3 Criar `src/app/(app)/configuracoes/layout.tsx` — Server Component layout que renderiza `<SettingsBreadcrumb />` acima de `{children}` dentro de wrapper `<div className="mx-auto max-w-[1200px]">`. NÃO inclui sidebar ou header (herdados de `(app)/layout.tsx`). Padding interno consistente com tokens de espaçamento do DS

## 5. Frontend — Settings Index Page (Integrações)

> **Design System Salvia**: h1 28px/600 "Integrações" (`text-text-primary`). Mesma estrutura visual do settings index: grid responsivo `grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6` (mesmo com 1 card em v1, grid já preparado para escalar). Cards `interactive` idênticos aos do índice principal (bg `surface`, border `border`, radius `xl`, shadow `xs`, padding `space-6`; hover border `border-strong`). v1 com 1 card: WhatsApp (`MessageCircle` 20px `text-text-secondary`, label h3 18px/600 "WhatsApp", description body-sm 13px/400 `text-text-secondary`).

- [ ] 5.1 Criar `src/app/(app)/configuracoes/integracoes/page.tsx` — Server Component que importa `INTEGRATIONS` e renderiza `<h1>Integrações</h1>` (28px/600) seguido do mesmo padrão de grid de cards usado pelo índice principal. Cada card usa o mesmo styling `Card interactive` e wrapper `<Link>`. Container `data-testid="integrations-index-page"`, cada card `data-testid="integration-card-{slug}"`. Focus ring, tap target e regras de a11y idênticas ao índice principal. NÃO adicionar `mx-auto max-w-[1200px]` — o layout pai provê
- [ ] 5.2 **Teste unitário:** Criar `src/__tests__/unit/app/(app)/configuracoes/integracoes/page.test.tsx` — verificar h1 "Integrações" renderiza, 1 card com testid `integration-card-whatsapp` renderiza com label, description e href corretos

## 6. Frontend — Lembretes Tabs Nav

> **Design System Salvia**: Tabs no padrão `Tabs underline` da seção Tabs do DS (não pills). Idle: `text-text-secondary`. Active: `text-text-primary` com border-bottom 2px `border-brand-500`. Padding `py-3 px-4` (`space-3` vertical, `space-4` horizontal). Mobile: container `overflow-x-auto` com `-webkit-overflow-scrolling: touch`, cada tab com min-height 44px (tap target a11y). Focus ring `focus-visible:shadow-focus focus-visible:outline-none`. Tabs como `<Link>` (não state-controlled) preservando deep-link, refresh, back/forward.

- [ ] 6.1 Criar `src/app/(app)/configuracoes/lembretes/lembretes-tabs.tsx` (Client Component, `'use client'`) — **Design system:** renderiza tab navigation usando `<Link>` elements estilizados como underline tabs. Lê `usePathname()` para determinar tab ativa. Tabs (em ordem): "Configuração" (href `/configuracoes/lembretes`, ativa em exact match), "Templates" (href `/configuracoes/lembretes/templates`, ativa em `startsWith` match — cobre `/templates/[templateKey]`), "Histórico" (href `/configuracoes/lembretes/historico`, ativa em exact match). Container `data-testid="lembretes-tabs"`, cada tab `data-testid="lembretes-tab-{slug}"` (slugs: `configuracao`, `templates`, `historico`). Styling: idle `text-text-secondary`, active `text-text-primary border-b-2 border-brand-500`. Padding `py-3 px-4`. Mobile: `overflow-x-auto` com touch scrolling, min-height 44px por tab. Focus ring `focus-visible:shadow-focus focus-visible:outline-none`
- [ ] 6.2 **Teste unitário:** Criar `src/__tests__/unit/app/(app)/configuracoes/lembretes/lembretes-tabs.test.tsx` — verificar 3 tabs renderizam com labels e hrefs corretos, verificar detecção de tab ativa para cada rota (exact match para configuracao/historico, startsWith para templates), verificar tab em rota de template edit `/configuracoes/lembretes/templates/lembrete_24h` marca "Templates" como ativa, verificar testids
- [ ] 6.3 Criar `src/app/(app)/configuracoes/lembretes/layout.tsx` — Server Component layout que renderiza `<LembretesTabs />` acima de `{children}`. Adicionar separação visual abaixo das tabs: `border-b border-border mb-6` (border-subtle separator + margin space-6). NÃO inclui breadcrumb (já provido pelo layout pai)

## 7. Frontend — Remove Duplicate Breadcrumb (Template Edit)

> **Design System Salvia**: o breadcrumb compartilhado do layout `configuracoes/layout.tsx` substitui o breadcrumb manual da página de edição de template. h1 da página permanece com label legível do template (microcopy do template).

- [ ] 7.1 Atualizar `src/app/(app)/configuracoes/lembretes/templates/[templateKey]/page.tsx` — remover o bloco manual `<nav aria-label="Breadcrumb">` (linhas com o `<ol>` breadcrumb). Manter o `<h1>` com label do template. Remover imports não utilizados (`ChevronRight`, `Link`) se ficarem órfãos. Não tocar em lógica de fetch/Server Actions

## 8. Frontend — Existing Page Adjustments

> **Design System Salvia**: pages internas de configurações deixam de aplicar seu próprio `mx-auto max-w-[1200px]` porque o layout `configuracoes/layout.tsx` agora provê. Evita double-constrain do max-width. h1 das pages permanece (page title é distinto do breadcrumb wayfinding).

- [ ] 8.1 Remover wrapper `<div className="mx-auto max-w-[1200px]">` das seguintes pages existentes (cada page renderiza conteúdo direto, sem o wrapper externo): `src/app/(app)/configuracoes/locais/page.tsx`, `src/app/(app)/configuracoes/agenda/page.tsx`, `src/app/(app)/configuracoes/lembretes/page.tsx`, `src/app/(app)/configuracoes/integracoes/whatsapp/page.tsx`, `src/app/(app)/configuracoes/lembretes/historico/page.tsx`, `src/app/(app)/configuracoes/lembretes/templates/page.tsx`, `src/app/(app)/configuracoes/lembretes/templates/[templateKey]/page.tsx`. Não tocar em h1, lógica de fetch ou Server Actions de cada page

## 9. E2E Tests

- [ ] 9.1 **Teste E2E:** Criar `src/__tests__/e2e/seeded/configuracoes-navigation.spec.ts` com test "sidebar Configurações navega para o índice" — login → click no item "Configurações" do sidebar → assertar URL é `/configuracoes` (não `/configuracoes/locais`) → assertar 4 cards visíveis com testids `settings-area-card-locais`, `settings-area-card-whatsapp`, `settings-area-card-lembretes`, `settings-area-card-agenda` e labels corretos (com diacríticos)
- [ ] 9.2 **Teste E2E:** Adicionar test "navegação por card para cada área de configurações" — para cada um dos 4 cards: click no card → assertar URL muda para o href esperado → assertar h1 da página alvo visível → voltar para `/configuracoes` (via breadcrumb "Configurações" ou navegação direta) entre cada
- [ ] 9.3 **Teste E2E:** Adicionar test "índice de integrações renderiza card WhatsApp e breadcrumb funciona" — navegar para `/configuracoes/integracoes` → assertar h1 "Integrações" e card WhatsApp visíveis. Assertar breadcrumb mostra "Configurações > Integrações" com "Configurações" linkado. Click no card WhatsApp → assertar URL é `/configuracoes/integracoes/whatsapp`
- [ ] 9.4 **Teste E2E:** Adicionar test "breadcrumb 'Integrações' link da página WhatsApp navega para índice de integrações sem 404" — navegar para `/configuracoes/integracoes/whatsapp` → click no segmento "Integrações" do breadcrumb → assertar URL é `/configuracoes/integracoes` → assertar h1 "Integrações" visível (NÃO página 404)
- [ ] 9.5 **Teste E2E:** Adicionar test "breadcrumb mostra trilha correta e links funcionam" — navegar para `/configuracoes/lembretes/templates` → assertar breadcrumb mostra "Configurações > Lembretes > Templates" → click "Configurações" → assertar URL `/configuracoes`. Navegar para `/configuracoes/integracoes/whatsapp` → assertar breadcrumb mostra "Configurações > Integrações > WhatsApp"
- [ ] 9.6 **Teste E2E:** Adicionar test "breadcrumb no índice mostra apenas root" — navegar para `/configuracoes` → assertar breadcrumb mostra "Configurações" como current page (não-linked, com `aria-current="page"`)
- [ ] 9.7 **Teste E2E:** Adicionar test "tabs do Lembretes alternam entre sub-seções" — navegar para `/configuracoes/lembretes` → assertar tab "Configuração" ativa (text-text-primary + border-b-2 border-brand-500). Click "Templates" → assertar URL é `/configuracoes/lembretes/templates` e "Templates" ativa. Click "Histórico" → assertar URL é `/configuracoes/lembretes/historico` e "Histórico" ativa
- [ ] 9.8 **Teste E2E:** Adicionar test "deep-link e back/forward das tabs do Lembretes" — navegar diretamente para `/configuracoes/lembretes/historico` → assertar tab "Histórico" ativa e conteúdo Histórico visível. Navegar para tab "Templates". Browser back → assertar tab "Histórico" ativa. Browser forward → assertar tab "Templates" ativa
- [ ] 9.9 **Teste E2E:** Adicionar test "tab Templates ativa em rota de template edit" — navegar para `/configuracoes/lembretes/templates/lembrete_24h` (ou outra chave seedada) → assertar tab "Templates" ativa (startsWith match)
- [ ] 9.10 **Teste E2E:** Adicionar test "página de edição de template não tem breadcrumb duplicado" — navegar para uma URL de edição de template (ex.: `/configuracoes/lembretes/templates/lembrete_24h`) → assertar apenas 1 elemento com `data-testid="settings-breadcrumb"` existe na página → assertar h1 ainda mostra label do template
- [ ] 9.11 **Teste E2E:** Adicionar test "viewport mobile mostra cards em coluna única e tap targets corretos" — definir viewport 375×667 → navegar para `/configuracoes` → assertar cards stackados em coluna única (cada card tem largura igual ao container menos padding) → assertar bounding box de cada card height ≥ 44px
- [ ] 9.12 **Teste E2E:** Adicionar test "tab bar do Lembretes scrolla horizontalmente em mobile" — definir viewport 375×667 → navegar para `/configuracoes/lembretes` → assertar tab bar tem `overflow-x-auto` ativo → assertar cada tab tem min-height 44px
- [ ] 9.13 **Teste E2E:** Adicionar test "navegação por teclado nos cards do índice" — navegar para `/configuracoes` → usar Tab para mover foco pelos 4 cards → assertar cada card focado tem indicador de foco visível (CSS `box-shadow` contém shadow-focus ou outline visível) → pressionar Enter no card "Lembretes" focado → assertar URL muda para `/configuracoes/lembretes`
- [ ] 9.14 **Teste E2E:** Adicionar test "navegação por teclado nas tabs do Lembretes" — navegar para `/configuracoes/lembretes` → usar Tab para mover foco pelas 3 tabs → assertar focus ring visível → pressionar Enter na tab "Templates" → assertar URL muda para `/configuracoes/lembretes/templates`
