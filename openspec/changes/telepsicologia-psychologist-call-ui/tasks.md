## 1. Middleware gating — add `/sessao` prefix

- [x] 1.1 In `src/middleware.ts`, add `'/sessao'` to the `APP_PREFIXES` array inside `classifyPath()`, so all `/sessao/*` routes are classified as `'app'` and gated by the auth decision table
- [x] 1.2 **Integration test:** Create `src/__tests__/integration/middleware/sessao-route-gating.int.test.ts` — negative-auth test: anonymous GET to `/sessao/fake-uuid/video` returns redirect to `/login`. Authenticated request passes through. Follow existing middleware integration test patterns

## 2. Server Actions — admitPatient + endVideoSession

- [ ] 2.1 Create `src/modules/telepsicologia/server/admit-patient.ts` — Server Action: (1) authenticate via supabase.auth.getUser(), (2) validate input (room_id: z.string().uuid()), (3) verify room ownership via RLS-scoped query, (4) verify room status is 'pending', (5) UPDATE video_rooms SET status='active', (6) INSERT video_session_logs event_type='patient_joined' (note: Stream handles actual admission — this action records the psychologist's intent and updates our DB state), (7) return { ok: true }
- [ ] 2.2 Create `src/modules/telepsicologia/server/end-video-session.ts` — Server Action: (1) authenticate via supabase.auth.getUser(), (2) validate input (room_id: z.string().uuid()), (3) verify room ownership, (4) call getStreamClient().video.call('default', streamCallId).end() wrapped in try/catch (Stream call may already be ended), (5) UPDATE video_rooms SET status='ended', (6) UPDATE sessions SET status='done', updated_at=now() WHERE id=room.session_id AND user_id=auth.uid(), (7) INSERT video_session_logs event_type='room_ended', (8) return { ok: true }. Sanitize Stream/DB errors
- [ ] 2.3 **Integration test:** Create `src/__tests__/integration/telepsicologia/admit-patient.int.test.ts` — mock Stream SDK. Tests: happy path updates status, room not owned -> rejected, room already active -> idempotent, unauthenticated -> rejected
- [ ] 2.4 **Integration test:** Create `src/__tests__/integration/telepsicologia/end-video-session.int.test.ts` — mock Stream SDK. Tests: happy path ends call + updates room + marks session done + logs event, room not owned -> rejected, room already ended -> idempotent, unauthenticated -> rejected

## 3. Video call page — Server Component

- [ ] 3.1 Create `src/app/(app)/sessao/[id]/video/page.tsx` — Server Component: (1) load session by id via RLS-scoped Drizzle query (WHERE id = params.id AND user_id from supabase.auth.getUser()), (2) verify session exists and modality='online', (3) load video_rooms row for this session, (4) if no room exists: show "Sala nao criada" with button to create (calls createVideoRoom), (5) if room exists: call getVideoToken() to mint psychologist JWT, (6) pass { streamCallId, token, apiKey: clientEnv.NEXT_PUBLIC_STREAM_API_KEY, session, patient, room } to VideoCallClient client component. Handle errors with redirect or error UI (never expose internal details)
- [ ] 3.2 Create `src/app/(app)/sessao/[id]/video/layout.tsx` — minimal layout that hides the main sidebar nav during the call. Renders children without the standard dashboard sidebar/header. Includes a back-to-agenda link

## 4. Video call client component — Stream SDK integration

- [ ] 4.1 Create `src/modules/telepsicologia/components/video-call-client.tsx` — `'use client'` component. Props: streamCallId, token, apiKey, userId, session, patient, room. (1) Initialize `StreamVideoClient` with apiKey, token, user: { id: userId, name: psychologistName }. (2) Create call instance: `client.call('default', streamCallId)`. (3) Wrap children in `<StreamVideo client={client}><StreamCall call={call}>`. (4) Use `useCallCallingState()` to switch between lobby/in-call/post-call views. (5) Cleanup: disconnect on unmount. Import Stream SDK CSS at this component level only
- [ ] 4.2 Create `src/modules/telepsicologia/components/pre-call-lobby.tsx` — `'use client'` component. Camera preview via `call.camera.enable()`, mic level indicator, device selection, "Entrar na sessao" button that calls `call.join()`. Show patient waiting indicator if participant count > 0. Handle permission denial with inline error message and troubleshooting instructions. Salvia tokens: Card default, radius-xl, Button primary lg
- [ ] 4.3 Create `src/modules/telepsicologia/components/in-call-view.tsx` — `'use client'` component. Uses Stream `SpeakerLayout` for video rendering. Positions psychologist PiP (bottom-right, radius-xl, shadow-md). Shows controls bar (CallControlBar component), elapsed time (top-left), connection quality indicator (top-right), waiting room badge (top-center, only if patient in lobby not yet joined)
- [ ] 4.4 Create `src/modules/telepsicologia/components/call-control-bar.tsx` — `'use client'` component. Buttons: ToggleAudioPublishingButton (mic), ToggleVideoPublishingButton (camera), ScreenShareButton (psych only), chat toggle (placeholder for change 4), end call (danger). All with aria-labels in PT-BR. Salvia: bg surface-muted, radius-2xl, gap space-2, ghost variant buttons + danger for end
- [ ] 4.5 Create `src/modules/telepsicologia/components/connection-quality-indicator.tsx` — `'use client'` component. Uses Stream's connection quality stats. 3-bar icon: green (success-500) = good, yellow (warning-500) = degraded, red (danger-500) = poor. Shows "Sua conexao esta instavel" banner when red (RF-09.13). Lucide icons: Signal/SignalLow/SignalZero
- [ ] 4.6 Create `src/modules/telepsicologia/components/elapsed-time.tsx` — `'use client'` component. Starts counting from call join time. Displays in caption style, text-tertiary. Format: "MM:SS" or "H:MM:SS" after 1h
- [ ] 4.7 Create `src/modules/telepsicologia/components/post-call-view.tsx` — `'use client'` component. AlertDialog: title "Sessao encerrada", description "Deseja registrar a evolucao agora?", actions: "Registrar evolucao" (Button primary, Link to /pacientes/[patientId]/prontuario/evolucoes), "Depois" (Button secondary, navigates to /agenda)
- [ ] 4.8 Create `src/modules/telepsicologia/components/end-call-dialog.tsx` — `'use client'` component. AlertDialog confirmation: "Encerrar sessao?" / "O paciente sera desconectado." / [Cancelar] [Encerrar sessao (danger)]. On confirm: calls endVideoSession Server Action, shows loading state

## 5. Agenda integration — "Iniciar video" button

- [ ] 5.1 In the agenda session card/detail component, add an "Iniciar video" button (Button secondary, Video icon from Lucide) for sessions where modality='online' AND status IN ('scheduled', 'confirmed'). Links to `/sessao/[sessionId]/video`. Conditionally visible (only online sessions)
- [ ] 5.2 **Unit test:** Create `src/__tests__/unit/modules/telepsicologia/components/pre-call-lobby.test.tsx` — mock Stream SDK hooks. Tests: camera preview renders, permission error shows inline message, "Entrar na sessao" button calls call.join()
- [ ] 5.3 **Unit test:** Create `src/__tests__/unit/modules/telepsicologia/components/end-call-dialog.test.tsx` — mock Server Action. Tests: dialog opens on trigger, cancel closes without action, confirm calls endVideoSession, loading state shown during action

## 6. Module barrel update

- [ ] 6.1 Update `src/modules/telepsicologia/index.ts` — add re-exports for new Server Actions (admitPatient, endVideoSession) and components (VideoCallClient, PreCallLobby, InCallView, CallControlBar, PostCallView, EndCallDialog, ConnectionQualityIndicator, ElapsedTime)
