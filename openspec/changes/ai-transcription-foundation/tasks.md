## 1. Env config

- [x] 1.1 In `src/shared/env/schemas.ts`, add to the server schema: `GEMINI_API_KEY: z.string().min(1)`, `GEMINI_MODEL_TRANSCRIPTION: z.string().regex(/^(gemini|gemma)-/).default('gemini-3.5-flash')`, `GEMINI_MODEL_NOTE: z.string().regex(/^(gemini|gemma)-/).default('gemini-3.5-flash')`, `AI_TRANSCRIPTION_BUCKET: z.string().default('ai-transcription-audio')`, `AI_TRANSCRIPTION_AUDIO_TTL_HOURS: z.coerce.number().int().min(24).max(168).default(24)`, `AI_TRANSCRIPTION_MAX_AUDIO_MB: z.coerce.number().int().min(1).max(500).default(200)`. Do NOT add any to the client schema.
- [x] 1.2 Update `src/shared/env/index.ts` to surface the new fields on `serverEnv`. Confirm `clientEnv` exports do NOT include them.
- [x] 1.3 Update `.env.example` with the six new variables, using descriptive placeholders (e.g., `GEMINI_API_KEY=<obtain at https://aistudio.google.com/apikey>`). Do NOT commit a real key.
- [x] 1.4 Add `GEMINI_API_KEY=<fake-for-test>` to the local docker compose env file used by `docker compose up` (only the test default key, never real).
- [x] 1.5 Unit test `src/__tests__/unit/shared/env/ai-transcription-env.test.ts`: (a) absence of `GEMINI_API_KEY` causes `parse` to throw with the field name in the message; (b) defaults apply for the five optional fields; (c) `AI_TRANSCRIPTION_AUDIO_TTL_HOURS=12` is rejected; (d) `clientEnv` schema does NOT contain `GEMINI_API_KEY`.

## 2. Drizzle schema and policies

- [x] 2.1 Create `src/shared/db/schema/ai-transcription/tables.ts` defining `aiTranscriptionSettings` and `aiTranscriptions` per the `ai-transcription-data-model` capability (columns, types, defaults, FKs, enums via `pgEnum` or `text` + Zod). Include `schemaVersion` is NOT a DB column — it lives inside the `generated_note` jsonb payload.
- [x] 2.2 Create `src/shared/db/schema/ai-transcription/policies.ts` exporting the SQL strings for: `ENABLE ROW LEVEL SECURITY` on each table; 4 policies per table (`SELECT`, `INSERT`, `UPDATE`, `DELETE`) using `user_id = auth.uid()`; storage bucket creation (`INSERT INTO storage.buckets (id, name, public) VALUES ('ai-transcription-audio','ai-transcription-audio', false) ON CONFLICT DO NOTHING`); 3 storage.objects policies (`SELECT`, `INSERT`, `DELETE`) scoped by `bucket_id = 'ai-transcription-audio' AND (storage.foldername(name))[1] = auth.uid()::text`.
- [x] 2.3 Create `src/shared/db/schema/ai-transcription/index.ts` re-exporting the table definitions and policy helpers.
- [x] 2.4 Update `src/shared/db/schema/index.ts` to re-export the new domain (add `export * from './ai-transcription'`).
- [x] 2.5 Run `npm run db:generate` to produce the Drizzle migration. Manually inspect the generated SQL: confirm `CREATE TABLE` for both tables, `ENABLE ROW LEVEL SECURITY` on both, 8 policies, 3 indexes (`idx_ai_transcriptions_user_status`, `idx_ai_transcriptions_user_created`, partial `idx_ai_transcriptions_audio_to_discard`). If `db:generate` does not emit RLS/policies/bucket SQL, append them by hand to the migration file inside `src/shared/db/migrations/`.
- [x] 2.6 Run `npm run db:migrate` against the local Postgres (docker compose). Confirm migration applies cleanly. Re-run to confirm idempotency (down + up via Drizzle CLI for the migration just added).
- [x] 2.7 Integration test `src/__tests__/integration/data-layer/ai-transcription-schema.int.test.ts` — using Testcontainers + Drizzle migrations: (a) `pg_policies` shows exactly 8 policies for the two tables, all referencing `auth.uid()`; (b) `pg_indexes` shows the 3 expected indexes; (c) the bucket exists and `public = false` in `storage.buckets`; (d) attempting to insert a row in `ai_transcriptions` with `patient_id = NULL` raises a NOT NULL violation; (e) attempting to update `status = 'archived'` fails the CHECK; (f) deleting an `evolutions` row sets the linked `ai_transcriptions.evolution_id` to NULL.
- [x] 2.8 Integration test `src/__tests__/integration/data-layer/ai-transcription-rls.int.test.ts` — two real Supabase JWTs (psychologists A and B). Negative scenarios: (a) B's RLS-scoped client cannot SELECT a transcription belonging to A; (b) B cannot INSERT a row forging `user_id = A.id` (`WITH CHECK` rejects); (c) B cannot UPDATE or DELETE A's row; (d) explicit positive: A reads only their own rows. Use the seeded e2e shared Postgres container helper.
- [x] 2.9 Integration test `src/__tests__/integration/data-layer/ai-transcription-storage-rls.int.test.ts`: upload an object as user A under `userA/...`; assert user B cannot `download` nor `list` that object via their RLS-scoped Supabase Storage client; assert A can.
- [x] 2.10 Integration test `src/__tests__/integration/data-layer/ai-transcription-discard-index.int.test.ts`: insert mixed rows (some with `audio_object_key IS NULL`, some discarded, some pending). Run `EXPLAIN (FORMAT JSON)` of the discard cron query. Assert plan uses `idx_ai_transcriptions_audio_to_discard` and does NOT contain a Seq Scan on `ai_transcriptions`.

## 3. Domain module skeleton

- [x] 3.1 Add `@google/genai` to `package.json` (`dependencies`, not devDependencies). Run `npm install`. Do NOT import it anywhere yet — only the dependency is added; the client lives in a downstream change.
- [x] 3.2 Create `src/modules/ai-transcription/lib/branded-types.ts` exporting `TranscriptionId` via `z.string().uuid().brand<'TranscriptionId'>()` and the inferred TS type.
- [x] 3.3 Create `src/modules/ai-transcription/lib/schemas.ts` exporting the canonical Zod enums (`TranscriptionStatusSchema`, `TranscriptionSourceSchema`, `TranscriptionTemplateSchema`, `RiskSensitivitySchema`) and the structured payloads (`GeneratedNoteSchema` with `schemaVersion: z.literal(1)` and all required fields per the `ai-transcription-module` capability; `RiskAlertSchema`).
- [x] 3.4 Unit test `src/__tests__/unit/modules/ai-transcription/lib/schemas.test.ts`: (a) `GeneratedNoteSchema.parse` accepts a valid payload; (b) rejects missing `schemaVersion`; (c) rejects `schemaVersion: 2`; (d) `RiskAlertSchema` rejects unknown `kind`; (e) `RiskAlertSchema.excerpt` rejects strings > 500 chars; (f) `TranscriptionId` cannot be assigned from a raw string at the type level (use `expectTypeOf` from Vitest).
- [x] 3.5 Create `src/modules/ai-transcription/lib/pseudonymize.ts` exporting `pseudonymizeTranscript({ patientFirstName, patientFullName, transcript }): string`. Implementation: split `patientFullName` into tokens; build a list of substitution targets = first name + every token from full name with `length > 2`; build a case-insensitive RegExp with `\b` word boundaries; replace all matches with `'Paciente'`. The helper MUST be pure (no logging, no I/O).
- [x] 3.6 Unit test `src/__tests__/unit/modules/ai-transcription/lib/pseudonymize.test.ts`: (a) replaces full name and first name; (b) case-insensitive; (c) does NOT replace short substrings (`lu` inside `luz`); (d) is idempotent on already-pseudonymized text; (e) handles transcripts that do not mention the patient (returns input unchanged); (f) does not mutate input; (g) does not log (mock the global logger and assert zero calls).
- [x] 3.7 Create `src/modules/ai-transcription/lib/logger.ts` exporting `createTranscriptionLogger(context: { transcriptionId?: TranscriptionId; userId?: string })` that returns a Pino child of the root logger with `redact: { paths: ['transcript','generatedNote','riskAlerts','patientName','patientFirstName','patientFullName','audioObjectKey','audioUrl','signedUrl','rawGeminiResponse','prompt'], censor: '[REDACTED]' }`.
- [x] 3.8 Unit test `src/__tests__/unit/modules/ai-transcription/lib/logger.test.ts` (capture stdout via pino destination buffer): (a) `patientName: 'Maria'` is serialized as `[REDACTED]` and the substring `Maria` never appears in the line; (b) `generatedNote` and `riskAlerts` redacted; (c) `transcriptionId` and `status` preserved in plaintext; (d) `prompt` and `audioObjectKey` redacted; (e) confirm log line is valid JSON.
- [x] 3.9 Create `src/modules/ai-transcription/server/index.ts` as a placeholder file with a comment explaining it will be populated by downstream changes (`ai-transcription-audio-upload`, `ai-transcription-gemini-processing`, etc.). Export nothing user-facing yet; an `export {}` keeps TS happy.
- [x] 3.10 Create `src/modules/ai-transcription/index.ts` (the module's PUBLIC barrel) re-exporting from `lib/branded-types`, `lib/schemas`, `lib/pseudonymize`, `lib/logger`, and `server`. This is the file external consumers must import from.
- [x] 3.11 Create `src/modules/ai-transcription/edge.ts` (the EDGE-SAFE entrypoint) re-exporting ONLY from `lib/branded-types` and `lib/schemas` (no logger, no helpers that pull pino/transports). The middleware does not consume the module today, but the file MUST exist so future UI changes can extend `middleware.ts:classifyPath()` against an edge-safe boundary.
- [x] 3.12 Unit test `src/__tests__/unit/modules/ai-transcription/edge-safety.test.ts`: dynamically `import('@/modules/ai-transcription/edge')` and assert the module loads without throwing in a sandbox that stubs Node-only modules (`node:crypto`, `pino` with destinations) to throw on access. Also assert `import '@/modules/ai-transcription'` exports `pseudonymizeTranscript`, `createTranscriptionLogger`, `GeneratedNoteSchema`, `RiskAlertSchema`, and the branded type.

## 4. ESLint hardening

- [x] 4.1 In `eslint.config.mjs`, add a `no-restricted-imports` rule scoped to files under `src/modules/ai-transcription/**` that forbids direct imports of `pino`, `pino-pretty`, and `@google/genai` outside the explicitly allowed files (`lib/logger.ts` may import pino; the future `gemini-client.ts` will be the only file allowed to import `@google/genai`, but for now no file in this change imports it).
- [x] 4.2 Confirm `npm run lint` is green after the rule addition. The existing rule that forbids direct `process.env` access stays in force.

## 5. Documentation

- [x] 5.1 Update `CLAUDE.md` "Folder conventions" section #1 to list `ai-transcription` as a new module (one-line mention next to `whatsapp/`, `medical-records/`, `telepsicologia/`).
- [x] 5.2 Update the project folder tree in `CLAUDE.md` to include `src/modules/ai-transcription/` and `src/shared/db/schema/ai-transcription/`.

## 6. Sanity check

- [ ] 6.1 Run `npm run lint`, `npm run typecheck`, `npm run test:unit`, `npm run test:integration` end-to-end. All green.
- [ ] 6.2 Verify the production build does not leak `GEMINI_API_KEY`: `npm run build` then `grep -r "GEMINI_API_KEY" .next/static 2>/dev/null` returns zero matches. Document the command in the change PR description.
