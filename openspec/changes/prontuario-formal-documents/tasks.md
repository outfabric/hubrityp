## 1. Database Schema + RLS + Migration

- [x] 1.1 Add `clinicalDocuments` table definition to `src/shared/db/schema/medical-records/tables.ts` (columns: id, user_id, patient_id, document_type, title, content, pdf_storage_path, pdf_size, digitally_signed, signature_method, status, references_cid10, cid10_consent_confirmed, finalized_at, created_at, updated_at; indexes: patient_type_created, status_finalized, user_id)
- [x] 1.2 Add RLS policies to `src/shared/db/schema/medical-records/policies.ts`: SELECT (owner), INSERT (owner), UPDATE (owner AND status='draft' USING clause), no DELETE. Export as `clinicalDocumentsPolicies`
- [x] 1.3 Update `src/shared/db/schema/medical-records/index.ts` barrel to re-export the new table
- [x] 1.4 Run `npm run db:generate`, manually append to the migration file: RLS enable + policies, FK constraints (user_id -> auth.users, patient_id -> patients), CHECK constraints (document_type, status, signature_method)
- [x] 1.5 Run `npm run db:migrate` locally and verify table exists with correct structure
- [x] 1.6 **Integration test:** Create `src/__tests__/integration/medical-records/clinical-documents-schema.int.test.ts` — verify: table exists, RLS enabled, expected policies exist (SELECT/INSERT/UPDATE, no DELETE), CHECK constraints on document_type/status/signature_method, UPDATE on finalized row returns 0 rows affected (RLS USING clause), psychologist B cannot SELECT/UPDATE psychologist A's documents

## 2. Zod Schemas Per Document Type + Unit Tests

- [x] 2.1 Create `src/modules/medical-records/lib/schemas/clinical-documents.ts` — discriminated Zod schemas: `baseDocumentContentSchema`, `declaracaoContentSchema`, `atestadoContentSchema`, `relatorioContentSchema`, `laudoContentSchema`, `parecerContentSchema`, `documentContentSchema` (discriminated union by type), `createDocumentInputSchema`, `updateDocumentInputSchema`, `finalizeDocumentInputSchema`, `DocumentType` enum, type exports via `z.infer`
- [x] 2.2 Implement `references_cid10` computation: `computeReferencesCid10(content)` returns true if `content.cid10Codes` has length > 0
- [x] 2.3 **Unit test:** Create `src/__tests__/unit/modules/medical-records/clinical-documents/schemas.test.ts` — validate: declaracao accepts without analise, laudo rejects without analise, atestado accepts period/validity, all types reject missing solicitante/demanda/procedimentos/conclusao/localData, cid10Codes array validated correctly, computeReferencesCid10 true with codes / false without

## 3. Server Actions — Create, Update, List, Detail

- [x] 3.1 Create `src/modules/medical-records/server/clinical-documents.ts` with `createDocument` — validates input with Zod, authenticates via `supabase.auth.getUser()`, snapshots psychologistInfo from user profile, inserts row with user_id from session, writes audit_log 'document.create'
- [x] 3.2 Implement `updateDocument` — validates input, authenticates, queries row (RLS ensures ownership), checks status='draft' (returns 'ALREADY_FINALIZED' otherwise), updates content + title + references_cid10, sets updated_at=now, writes audit_log 'document.update'
- [x] 3.3 Implement `listDocumentsByPatient` — validates patientId, authenticates, queries with optional type/status filters, orders by created_at DESC, writes audit_log 'document.list'
- [x] 3.4 Implement `getDocumentDetail` — validates documentId, authenticates, returns full document (RLS ensures ownership), writes audit_log 'document.view'
- [x] 3.5 **Integration test:** Create `src/__tests__/integration/medical-records/clinical-documents-crud.int.test.ts` — test: createDocument persists row + audit entry, updateDocument works for draft (content updated), updateDocument rejects for finalized (code='ALREADY_FINALIZED'), listDocumentsByPatient returns owned docs with correct order, filter by type and status works, getDocumentDetail returns full content, RLS negative: psychologist B cannot read/update psychologist A's documents, audit_log entries written for every operation

## 4. Finalize Action + CID-10 Consent Gate + Integration Tests

- [x] 4.1 Implement `finalizeDocument` — validates documentId + cid10ConsentConfirmed, authenticates, queries document (must be draft), validates per-type required sections (laudo/relatorio/parecer require analise), computes references_cid10, if true and cid10ConsentConfirmed !== true reject with 'CID10_CONSENT_REQUIRED', sets status='finalized' + finalized_at=now() + references_cid10 + cid10_consent_confirmed, enqueues Inngest event 'documents/pdf.requested' with documentId, writes audit_log 'document.finalize'
- [x] 4.2 **Integration test:** Create `src/__tests__/integration/medical-records/clinical-documents-finalize.int.test.ts` — test: finalize without CID-10 succeeds (status=finalized, event published), finalize with CID-10 + consent=true succeeds, finalize with CID-10 + consent missing/false rejected ('CID10_CONSENT_REQUIRED'), finalize laudo without analise rejected ('VALIDATION_ERROR'), finalize already-finalized rejected ('ALREADY_FINALIZED'), Inngest event 'documents/pdf.requested' is sent (mock assert), audit_log entry created

## 5. PDF Builder Pure Functions + Unit Tests

- [x] 5.1 Create `src/modules/medical-records/lib/pdf/html-to-text.ts` — minimal Tiptap HTML to plain text converter: strips tags, preserves line breaks for `<p>`, converts `<li>` to "- " prefixed lines, handles `<strong>` and `<em>` (text only in PDF, no rich styling beyond bold/italic), returns sanitized text
- [x] 5.2 Create `src/modules/medical-records/lib/pdf/pdf-helpers.ts` — pure helper functions: `buildHeader(doc, psychologistInfo)`, `buildTitle(doc, documentType, title)`, `buildSection(doc, label, htmlContent)`, `buildCid10Section(doc, codes)`, `buildLocalData(doc, localData)`, `buildSignatureBlock(doc, crp)`, `addPageNumber(doc, current, total)`, `addWatermark(doc)`, `collectBuffer(doc): Promise<Buffer>`
- [x] 5.3 Create `src/modules/medical-records/lib/pdf/build-clinical-document-pdf.ts` — main orchestrator function `buildClinicalDocumentPdf(input): Promise<Buffer>` using pdfkit with bufferPages:true, calls helpers in sequence, applies page numbering and watermark via bufferedPageRange loop
- [x] 5.4 **Unit test:** Create `src/__tests__/unit/modules/medical-records/clinical-documents/html-to-text.test.ts` — test: strips `<p>` tags to newlines, converts `<li>` to bullet lines, handles nested tags, returns empty string for null/undefined input
- [x] 5.5 **Unit test:** Create `src/__tests__/unit/modules/medical-records/clinical-documents/pdf-builder.test.ts` — test: `buildClinicalDocumentPdf` returns a non-empty Buffer for valid input, output size is deterministic for fixed input (snapshot test on byte-count range), each helper function produces expected pdfkit calls (via mock doc object for helpers: addPageNumber adds text at expected position, addWatermark applies opacity + rotate, buildSignatureBlock includes CRP number)

## 6. Inngest Job — Generate PDF + Integration with Mock Storage

- [x] 6.1 Create `src/modules/medical-records/inngest/client.ts` — Inngest client with typed event 'documents/pdf.requested' (data: { documentId: string })
- [x] 6.2 Create `src/modules/medical-records/inngest/generate-document-pdf.ts` — Inngest function with id='documents/generate-pdf', idempotency on event.data.documentId, 5 steps: read-document (service-role, return early if pdf_storage_path set), build-pdf (call buildClinicalDocumentPdf), upload-to-storage (service-role Storage client, path=${user_id}/${patient_id}/${documentId}.pdf), update-document-row (set pdf_storage_path + pdf_size, service-role justified comment), write-audit-log
- [x] 6.3 Register the Inngest function in `src/app/api/inngest/route.ts` (add to the serve functions array)
- [x] 6.4 **Integration test:** Create `src/__tests__/integration/medical-records/clinical-documents-pdf-job.int.test.ts` — test: job reads document row, builds PDF (non-empty buffer), uploads to mock Storage (assert correct path), updates row with pdf_storage_path and pdf_size, writes audit_log 'document.pdf-generated', idempotent: second run with pdf_storage_path already set returns early without re-upload

## 7. Signed URL Action + Integration Test

- [x] 7.1 Implement `getDocumentPdfUrl` in `src/modules/medical-records/server/clinical-documents.ts` — validates documentId, authenticates, queries document (RLS ownership), checks pdf_storage_path is set (else 'PDF_NOT_READY'), generates signed URL from Supabase Storage with 300s expiry, writes audit_log 'document.pdf-download', returns { signedUrl, expiresIn: 300 }
- [x] 7.2 **Integration test:** Add to `src/__tests__/integration/medical-records/clinical-documents-crud.int.test.ts` (or separate file) — test: returns signed URL when pdf_storage_path set, rejects with 'PDF_NOT_READY' when pdf_storage_path null, RLS negative: psychologist B cannot get URL for A's document, audit_log entry written

## 8. Frontend — List + Type Selector + Editor + Finalize Modal + Viewer

- [x] 8.1 Create `src/modules/medical-records/components/documents-empty-state.tsx` — Salvia empty state: FileText icon in text-tertiary, h4 "Nenhum documento", description "Crie declaracoes, atestados, laudos e outros documentos formais.", Button primary "Novo documento"
- [x] 8.2 Create `src/modules/medical-records/components/document-card.tsx` — Card (radius xl, shadow xs, padding space-6) with: type icon (per mapping), title, created date in body-sm text-tertiary, status Badge (neutral='Rascunho', success='Finalizado'), action buttons: "Editar" Link (draft) / "Visualizar" Link (finalized) / "Baixar PDF" button with Download icon (finalized + pdf ready)
- [x] 8.3 Create `src/modules/medical-records/components/documents-tab.tsx` — Tab content: header with h3 "Documentos clinicos" + Button primary [FilePlus2] "Novo documento" (links to /documentos/novo), filter row (Select for type, Badge group for status filter), DocumentCard list or empty state. Replaces EmptyTabPlaceholder for "Documentos" tab in prontuario-tabs.tsx
- [x] 8.4 Create `src/modules/medical-records/components/document-type-selector.tsx` — 2-col grid (1-col mobile) of 5 interactive Cards, each with: Lucide icon + type label + 1-line description. On click: calls createDocument server action with selected type, navigates to /documentos/[newDocId]. Loading state on the clicked card
- [x] 8.5 Create `src/modules/medical-records/components/document-editor.tsx` (Client Component) — structured editor: title input with auto-derived placeholder, status Badge "Rascunho", sections per document type (Solicitante, Demanda, Procedimentos, Analise, Conclusao, Local e Data, CID-10), each section: label (aria-labelledby) + Tiptap editor instance (reuse config), CID-10 section uses Cid10Combobox from change #2, auto-save via useAutoSave (10s), AutoSaveIndicator. Buttons: "Finalizar e gerar PDF" primary (disabled if required fields missing), "Salvar rascunho" link, disabled "Assinar com e-CPF" + Badge "Em breve"
- [x] 8.6 Create `src/modules/medical-records/components/finalize-modal.tsx` — destructive Modal: text "Apos finalizacao, este documento nao podera ser editado. Uma nova versao exige criar um novo documento." If references_cid10: Checkbox "Confirmo que o paciente consentiu com a inclusao do(s) codigo(s) CID-10 (RN-05.06)." with AlertTriangle icon. Confirm button disabled until checkbox checked (when applicable). Focus trap, Escape closes. On confirm: calls finalizeDocument, shows toast "Gerando PDF...", polls/subscribes for pdf_storage_path, toast "PDF pronto" + download link
- [x] 8.7 Create `src/modules/medical-records/components/document-viewer.tsx` — read-only rendering of all sections (non-interactive Tiptap content or plain rendered HTML), header with title + Badge "Finalizado" + Lock icon + message "Este documento foi finalizado e nao pode ser editado." Buttons: "Baixar PDF" primary [Download] (calls getDocumentPdfUrl, opens in new tab), "Criar novo documento similar" secondary (calls createDocument with cloned content, navigates to new editor), disabled "Assinar com e-CPF" + Badge "Em breve"
- [x] 8.8 Update `src/modules/medical-records/components/prontuario-tabs.tsx` — replace EmptyTabPlaceholder for "Documentos" tab with DocumentsTab component (lazy-loaded)
- [x] 8.9 Create `src/app/(app)/pacientes/[id]/prontuario/documentos/novo/page.tsx` (RSC) — renders DocumentTypeSelector. Validates patient ownership server-side before rendering
- [x] 8.10 Create `src/app/(app)/pacientes/[id]/prontuario/documentos/[docId]/page.tsx` (RSC) — fetches document detail server-side, validates ownership. If status='draft': renders DocumentEditor. If status='finalized': renders DocumentViewer
- [x] 8.11 Update barrel `src/modules/medical-records/index.ts` — re-export new server actions (createDocument, updateDocument, finalizeDocument, listDocumentsByPatient, getDocumentDetail, getDocumentPdfUrl), Zod schemas, document type constants

## 9. End-to-End Tests

- [ ] 9.1 **E2E (Playwright, seeded):** Create `src/__tests__/e2e/seeded/prontuario/clinical-documents.spec.ts` — happy path laudo: navigate to patient prontuario > Documentos tab > "Novo documento" > select "Laudo psicologico" > fill all sections (solicitante, demanda, procedimentos, analise, conclusao, local e data) > add CID-10 code F32 via combobox > click "Finalizar e gerar PDF" > verify consent checkbox appears > check it > confirm > wait for PDF ready (mock Inngest runs synchronously in e2e) > click "Baixar PDF" > verify PDF is non-empty (check response Content-Type or size)
- [ ] 9.2 **E2E (Playwright, seeded):** Attempt to edit finalized document — navigate to a finalized document > verify editor is in read-only mode (no editable inputs, no save button visible), "Baixar PDF" button visible, "Criar novo documento similar" button visible
- [ ] 9.3 **E2E (Playwright, seeded):** Filter list by type and status — navigate to Documentos tab with multiple documents seeded > apply type filter "Laudo" > verify only laudos shown > apply status filter "Finalizado" > verify only finalized documents shown > clear filters > verify all documents shown
