# Tasks

> Sequencing rule (mandatory): every automated test task is placed IMMEDIATELY
> AFTER the code task that motivates it, so the implementing agent still has the
> context of that change when writing the test. Do NOT batch tests at the end.

## 1. Data model — arrival + liveness columns + audit event

- [x] 1.1 Add two columns to the `videoRooms` table in `src/shared/db/schema/telepsicologia/tables.ts`: `patientWaitingAt` (`patient_waiting_at timestamptz`, nullable) — IMMUTABLE first-arrival marker (set-once; audit + wait-time); and `patientLastSeenAt` (`patient_last_seen_at timestamptz`, nullable) — MUTABLE liveness heartbeat (advanced every waiting poll; cleared to NULL on departure; watched by the broadcast trigger; seeds the psychologist presence badge). Document both roles with `why` comments. No new index (read by PK, watched in-row).
- [x] 1.2 Add `patient_arrived` to the `video_session_logs` `event_type` CHECK constraint in the same file; update the table's doc comment to note it is the arrival event, distinct from admission `patient_joined`. (Optionally also add `patient_left` if the departure route will log it — see 4.1; only add the CHECK value if you implement the log.)
- [x] 1.3 Generate the migration (`npm run db:generate`) and manually edit it to (a) add both columns and (b) swap the CHECK constraint to include `patient_arrived` (drop + recreate the constraint). Update `src/shared/db/migrations/README.md` migrations index with the new file.
- [x] 1.4 Integration test (`*.int.test.ts`, Testcontainers + real Postgres + Drizzle migrations): assert `video_rooms` accepts `patient_waiting_at=NULL` and `patient_last_seen_at=NULL` by default and timestamp values; assert advancing `patient_last_seen_at` repeatedly leaves `patient_waiting_at` fixed; assert clearing `patient_last_seen_at` back to NULL leaves `patient_waiting_at` intact; assert `video_session_logs` accepts `event_type='patient_arrived'` and rejects an invalid event type. Co-locate under `src/__tests__/integration/telepsicologia/`.

## 2. Broadcast from Database — heartbeat trigger + Realtime authorization

- [x] 2.1 Author the `realtime.messages` owner-scoped SELECT (receive) policy in `src/shared/db/schema/telepsicologia/policies.ts` (new exported policy array), authorizing topic `video-room:<roomId>` only when `EXISTS (SELECT 1 FROM video_rooms WHERE id = <topic room id> AND user_id = auth.uid())` and `extension = 'broadcast'`. Add a `why` comment: first private Realtime channel in the repo; payload is non-sensitive even if the policy were too broad.
- [x] 2.2 Append to the migration from 1.3 (manually, per migrations README): the SECURITY DEFINER trigger function that, on `video_rooms` UPDATE, calls `realtime.send` with a hand-built minimal JSONB payload `{ room_id, last_seen_at }` on topic `video-room:<id>` (private), plus the trigger with `WHEN (NEW.patient_last_seen_at IS DISTINCT FROM OLD.patient_last_seen_at)`. `IS DISTINCT FROM` treats NULL as a value, so a departure (timestamp → NULL) ALSO fires and broadcasts `{ room_id, last_seen_at: null }` — no second event type needed. Also append the `realtime.messages` policy from 2.1. Do NOT use `realtime.broadcast_changes` (it dumps the full row including `patient_jwt`/tokens). Wrap the trigger/function/policy creation in `DO $$ … IF EXISTS (realtime schema / realtime.send) … $$` guards so a plain-Postgres (Testcontainers/CI) migration is a no-op for these objects, mirroring migration `0039`. Confirm the exact `realtime.send` signature and `realtime.topic()`/`realtime.messages` predicate against the project's local Supabase version before finalizing (Context7/Supabase docs).
- [x] 2.3 Confirm the policy-coverage contract still passes (the policy is declared in `policies.ts` and present in the migration). If `realtime.messages` is outside the contract's scanned set, document why in the policy file comment.
- [x] 2.4 Integration test (Testcontainers): on a stack where the `realtime` schema is available, assert the trigger fires on each `patient_last_seen_at` change — including a clear back to NULL (departure), where the broadcast payload's `last_seen_at` is null — and is silent on updates that do not change `patient_last_seen_at` (e.g. status change to `active` only); assert the payload carries only `room_id` and `last_seen_at` (no jwt/token/PII). If the local Testcontainers image lacks the `realtime` schema, state that explicitly in the test (skip with a documented reason per CLAUDE.md) and cover the guard-no-op path instead (migration applies cleanly; trigger absent).
- [x] 2.5 Integration test (RLS): assert the `realtime.messages` policy authorizes the owner for `video-room:<ownedRoomId>` and denies a different authenticated user (negative-auth). If receipt cannot be driven headlessly in the test stack, assert the policy predicate directly (e.g. `SELECT` against the policy condition for owner vs non-owner) and note the limitation.

## 3. Patient join route — heartbeat + set-once arrival

- [x] 3.1 In `src/app/api/video/join/route.ts`, on the `waiting` branch only, advance liveness on EVERY poll and set first-arrival once. Use a single statement (`UPDATE video_rooms SET patient_last_seen_at = now(), patient_waiting_at = COALESCE(patient_waiting_at, now()) WHERE (patient_token = :token OR partner_token = :token) RETURNING …`) — or, if cleaner in Drizzle, a `SELECT … FOR UPDATE` pre-check + UPDATE inside one transaction. Detect "first arrival" (was `patient_waiting_at` NULL before this poll?) and, only then, insert exactly one `video_session_logs` row `event_type='patient_arrived'` with `participant_role` = patient/partner based on which token matched. Use server `now()` for a single consistent clock. Keep the `waiting` response body unchanged (`status`, `psychologistName`, `psychologistPhotoUrl` only). Add this AFTER the existing status checks so `too_early`/`active`/`410` paths never write either timestamp. No new internal IDs/JWTs/timestamps in the response. Log errors without PII.
- [x] 3.2 Integration test (Testcontainers + real Postgres) for `/api/video/join`: first waiting poll sets BOTH `patient_waiting_at` and `patient_last_seen_at` and inserts exactly one `patient_arrived` log; repeated polls advance `patient_last_seen_at` while `patient_waiting_at` stays fixed and no duplicate log is inserted; a re-arrival poll after a prior departure (heartbeat NULL, `patient_waiting_at` still set) re-stamps `patient_last_seen_at` and does NOT insert a second `patient_arrived` log; partner-token first arrival records `participant_role='partner'`; `too_early`/`active`/ended branches write neither timestamp; the `waiting` response body contains only the three allowed fields (assert absence of ids/tokens/jwt/timestamps).

## 4. Patient departure — `/api/video/depart` route + mandatory `pagehide` beacon

- [ ] 4.1 Create `src/app/api/video/depart/route.ts` (Node runtime, `force-dynamic`, mirroring the join handler's security shape). Public-but-token-gated: per-IP rate limit (`createRateLimiter`) BEFORE any DB work; parse + Zod-validate the body with the same `{ token }` shape (length 64, `^[a-f0-9]+$`). On a valid token, `UPDATE video_rooms SET patient_last_seen_at = NULL WHERE (patient_token = :token OR partner_token = :token) AND status = 'pending' AND patient_last_seen_at IS NOT NULL` (guards: idempotent, and never disturbs an already-`active` room). Do NOT touch `patient_waiting_at`. (Optional: insert a `patient_left` log — only if you added the CHECK value in 1.2.) Return an opaque empty 200/204 with `no-store`; expose no IDs/tokens/JWT/PII; log errors without PII. `sendBeacon` ignores the response.
- [ ] 4.2 Integration test (Testcontainers + real Postgres) for `/api/video/depart`: valid token on a `pending` room with a set heartbeat clears `patient_last_seen_at` to NULL and leaves `patient_waiting_at` unchanged; a second (duplicate) beacon updates zero rows (idempotent); a beacon for an `active` room does NOT clear liveness; an invalid/malformed token is rejected with no row updated; the response body exposes nothing (no ids/tokens/jwt/PII); rate limiting rejects over-limit requests before DB work.
- [ ] 4.3 Wire the mandatory departure beacon in `src/modules/telepsicologia/components/waiting-room-view.tsx`: register a `pagehide` listener (NOT `beforeunload`/`unload`, for mobile + bfcache reliability) that calls `navigator.sendBeacon('/api/video/depart', <body with the token>)` once; clean up the listener on unmount. (Optionally also fire on `visibilitychange`→`hidden`; safe because the endpoint is idempotent.) Use a `Blob`/`FormData`/JSON body the route can parse; the token is the props `token`. Document why `pagehide` over `beforeunload`.
- [ ] 4.4 Unit test (Vitest + RTL) for the beacon wiring: mock `navigator.sendBeacon`; mount the waiting-room view; dispatch a `pagehide` event; assert `sendBeacon` is called once with `/api/video/depart` and a body carrying the token; assert the listener is removed on unmount (no call after unmount).

## 5. Psychologist UI — presence (heartbeat freshness + TTL), departure clear, post-admit clear

- [ ] 5.1 Define `WAITING_PRESENCE_TTL_MS = 30_000` as a named, exported constant under `src/modules/telepsicologia/lib/` with a `why` comment (10s poll → tolerate ~2 missed heartbeats). Server-render `patient_last_seen_at` into the video page: ensure the `room` row passed to `video-call-client.tsx` includes `patientLastSeenAt` (part of `VideoRoom` once 1.1 lands; verify the page query selects it / passes the full row).
- [ ] 5.2 Create an owner-scoped client presence hook `useVideoRoomPresence` under `src/modules/telepsicologia/hooks/` (PRIVATE channel): `createBrowserClient().channel('video-room:'+roomId, { config: { private: true } }).on('broadcast', { event: <heartbeat event> }, …).subscribe()`. Keep `lastSeenAt` state (seeded from the server-rendered value); on each heartbeat record the timestamp; **on a broadcast whose `last_seen_at` is null (departure), set `lastSeenAt = null` so presence becomes false immediately**. Run a periodic re-evaluation (interval) computing `isPatientPresent = lastSeenAt != null && (Date.now() - lastSeenAt) < WAITING_PRESENCE_TTL_MS` so the badge auto-clears after the TTL when heartbeats stop. Accept an injectable/overridable TTL to enable fast tests. Tear down channel + interval on unmount; no-op when ids are falsy. Treat payload as untrusted (record timestamp/null only; never an authz decision). Export via the module barrel if consumed outside the module.
- [ ] 5.3 Unit test for `useVideoRoomPresence` (Vitest + RTL `renderHook`, mocked browser client, fake timers): present when seeded with a fresh `lastSeenAt`; a heartbeat resets freshness and keeps it present; **a null-`last_seen_at` broadcast clears presence immediately (before the TTL)**; clears after the TTL elapses with no further heartbeats; ignores malformed payloads; unsubscribes + clears the interval on unmount; no-op with falsy roomId/userId.
- [ ] 5.4 Update `in-call-view.tsx`: replace `const showWaitingBadge = room.status === 'pending' && participantCount <= 1` with presence-driven logic — `room.status === 'pending' && isPatientPresent`, where `isPatientPresent` comes from `useVideoRoomPresence` seeded by `room.patientLastSeenAt`, wired with `roomId = room.id` and the psychologist `userId` (`currentUser.id`). Remove the `participantCount` term. Add a local post-admit clear: on `onAdmitPatient` success, flip local state (e.g. an `admitted` boolean) so the badge gate becomes false immediately, independent of the static `room.status` prop. **Fix the stale comment** ("participants beyond the psychologist … in the lobby") to describe the heartbeat/TTL + departure + post-admit logic.
- [ ] 5.5 Unit test for the badge logic (Vitest + RTL, fake timers): badge hidden when no/stale heartbeat even while `status==='pending'` and `participantCount===1`; badge shown when heartbeat is fresh and `status==='pending'`; badge clears immediately on a departure (null heartbeat) broadcast; badge auto-clears after the TTL when heartbeats stop; badge clears immediately after a successful admit (without a `room.status` prop change). (Mock Stream hooks and `useVideoRoomPresence` as needed.)

## 6. End-to-end (seeded Playwright)

- [ ] 6.1 E2E seeded spec under `src/__tests__/e2e/seeded/telepsicologia/`: with a seeded online session whose room is `pending`, assert the psychologist's badge is NOT shown before any patient arrival; drive a patient `POST /api/video/join` (waiting) so the heartbeat is recorded; assert the psychologist sees "Paciente aguardando" + "Admitir" (via server-render seed on reload and/or live broadcast). Then assert the badge CLEARS IMMEDIATELY when a departure is signalled (drive `POST /api/video/depart` or dispatch `pagehide`) — distinct from the TTL path. Separately assert the no-beacon path: presence auto-clears after the TTL when heartbeats simply stop (use a shortened/injectable TTL or fake timers so the test does not wait 30s). Assert the post-admit clear (badge disappears immediately after clicking "Admitir"). Include the negative case (no arrival → no badge) and the early-arrive-then-leave case. Note any realtime-stack limitation per CLAUDE.md and fall back to the server-render-seed + TTL-expiry path if live broadcast is not exercisable in the seeded stack.

